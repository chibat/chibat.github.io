{
  "tasks": {
    "start": "deno run --allow-net=:4507 --allow-read --allow-sys jsr:@std/http@0.224.4/file-server docs",
    "deploy": "git pull && git add . && git commit -m deploy && git push"
  }
}
